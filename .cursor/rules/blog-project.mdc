---
description: SSHeRun 博客项目的开发规范和约束
globs:
  - "**/*"
---

# 项目规范

## 技术架构
- 框架：Astro 6.x，静态站点生成（SSG）
- 部署：GitHub Pages，通过 GitHub Actions 自动构建（push main 触发）
- Node >= 22.12.0
- 安装依赖必须使用 `--legacy-peer-deps`（astro-mermaid peer dep 兼容性）

## 文章规范
- 文章存放在 `src/content/blog/` 下，格式为 Markdown (.md)
- Frontmatter 必须包含：title, description, pubDate
- 可选字段：updatedDate, heroImage, tags
- heroImage 路径格式：`../../assets/xxx.jpg`
- 文件名即 slug，用于 URL（如 `hello-world.md` → `/blog/hello-world/`）

## Markdown 扩展
- 支持 Obsidian 双向链接 `[[slug]]` `[[slug|text]]` `[[slug#heading]]`
- 支持 Mermaid 图表（```mermaid 代码块）
- Wikilink 中的文章名必须与 `src/content/blog/` 下的文件名（不含扩展名）一致

## Agent 友好端点
- `/llms.txt` — 站点概览（动态生成，勿手动创建 public/llms.txt）
- `/llms-full.txt` — 全文聚合
- `/blog/{slug}.md` — 单篇纯 Markdown
- 这些端点从内容集合自动生成，不需要手动维护

## 设计原则
- 博客主题："做一个对 Agent 友好的博客"
- 中文优先（日期 zh-CN、评论中文、排版针对中文优化）
- 暗色模式通过 CSS `prefers-color-scheme` 自动切换
- 尽量用自定义 remark 插件而不是引入第三方包
- 样式变量定义在 `src/styles/global.css` 的 `:root` 中

## 关键文件
- `astro.config.mjs` — Astro 配置和 remark 插件注册
- `src/plugins/remark-wikilinks.mjs` — Obsidian wikilinks 解析插件
- `src/content.config.ts` — 内容集合 Schema
- `src/components/Giscus.astro` — 评论系统配置（GitHub Discussions）
- `.github/workflows/deploy.yml` — CI/CD 流水线
